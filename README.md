# Roman Numerals Converter

Command-line application written in Golang that converts a cardinal number into a Roman numeral.

### Requirements

- Go **1.22.2 or higher** installed.
- `make` available in your system.

### Installing (Globally)

```bash
make install
```

## Running
```bash
make run ARGS="--number=100"
```
